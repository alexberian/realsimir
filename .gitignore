# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.ipynb_checkpoints/

# Environments
.env
.venv/
venv/
env/

# Data / model artifacts (keep out of git; real data lives in /workspace/data/)
# Anchored: this is about a data directory at the repo root, not about
# realsimir/data/, which is the online data generator of step 07.
/data/
/datasets/
# cached detector passes -- CropIndex.save output, rebuilt with
# `python -m realsimir.data index`
boxes/
*.npy
*.npz
*.h5
*.hdf5
*.pt
*.pth
*.ckpt
*.pkl
*.safetensors

# Training outputs
runs/
outputs/
checkpoints/
logs/
wandb/
*.weights

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
