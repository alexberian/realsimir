the goal:
generate realistic IR images from unlabeled synthetic IR images of ships. We also have real IR data of ships, sometimes the real mages have many ships.

the idea:
start with a foundation diffusion pretrained model, such as edm2. re train on the unlabeled real data in an unsupervised manner. to make a training image, we generate a bounding box of the ship using some bounding box algorithm. jitter the box by making it 10 or 20% larget than the ship. paste it back into the image slightly offset or rotated pixel gain changed or noised or whatever else. crop out a 256x256 image in the same spot from the doctored and original image. then we train the model to output the clean image. in this way the model is learning to clean poorly cropped images. then we can take croppings of the synthetic IR ship images and paste them into realworld images, then ask the model to do what it does best, clean poor-looking croppings. we train the diffusion model with noised versions of the target image of varying noise levels.

where to get data/models:
do not modify anything in /workspace/data/
synthetic ir data is in /workspace/data/arete_ir_sim_data/
real ir data is in /workspace/data/open_ir_images/
diffusion model: https://github.com/NVlabs/edm2
bounding box model: https://github.com/ayooshkathuria/pytorch-yolo-v3
  ^ a placeholder -- COCO 'boat' has never seen an IR image. The detector is a plug-in
    (realsimir/detectors/), picked by name: ShipCropper("yolov3") / "arete" / "precomputed",
    or "module:ClassName" for a model living outside this repo. See that package's
    docstring for how to add one, and `python -m realsimir.detectors check <name>` to
    validate it before training on its boxes.

modules:
ship bounding box detection object
data augmentation object
online data generator object
diffusion model
training object
test object

todo list:
 DONE 01.  write description of the project and algorithm
 DONE 02.  start a git repo
 DONE 03.  download bounding box and diffusion model code bases into their own folders
 DONE 04.  create a conda environment that can support both the diffusion and the bounding box model
 DONE 05.  create ship cropping object  (realsimir/ship_cropper.py, detectors in
           realsimir/detectors/; demo figures: python main.py [--detector NAME] -> ./figures;
           tests: python -m pytest tests)
 DONE 06.  create data augmentation object  (realsimir/augment/: ShipAugmenter builds the
           (clean, doctored) pair, ops.py holds the corruptions and a registry so the
           training config can name them.  aug(crop, seed=i) -> pair.doctored/.clean/.mask;
           pasting a synthetic chunk into a real frame is the same call with source=.
           python -m realsimir.augment list | check; figures 06-08 from python main.py)
 DONE 07.  create data generator object  (realsimir/data/: ShipDataGenerator streams
           (clean, doctored, mask) pairs online -- boxes come from a CropIndex detected
           once and cached, pixels and corruption come per call, and every example is
           seeded from (run seed, epoch, index) so workers cannot change the data.
           CompositeGenerator is the test-time stream, two kinds: synthetic ships into
           real frames (what the model is for, no target) and .self_paste(), the same
           ships back in their own renders by the training recipe, which does have a
           target so the model can be scored.  Placed ships are rolled at most
           max_rotation_deg (2) -- a hull sits level on the water.  figure 09 from
           python main.py is the self-paste test set; 08 is the same paste into real
           frames.
           pools.py names the corpora ('real', 'raysense', 'arete'); torch_dataset.py is
           the DataLoader adapter, uint8 NCHW the way edm2's encoder wants it.
           python -m realsimir.data pools | index | check | stats)
 08.  view test images before training
 09.  create training script with wandb logging
 10.  train diffusion model
